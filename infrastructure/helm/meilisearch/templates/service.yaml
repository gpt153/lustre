apiVersion: v1
kind: Service
metadata:
  name: {{ include "lustre-meilisearch.fullname" . }}
  labels:
    {{- include "lustre-meilisearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "lustre-meilisearch.selectorLabels" . | nindent 4 }}
