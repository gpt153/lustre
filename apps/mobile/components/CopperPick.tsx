import { View } from 'react-native'

interface CopperPickProps {
  profile: any
  onLike: () => void
  onPass: () => void
  onViewProfile: () => void
}

export function CopperPick(_props: CopperPickProps) {
  return <View />
}
