import { View } from 'react-native'

/**
 * Placeholder screen for the center-action tab.
 * This screen is never shown — the tabPress event is intercepted
 * to open the QuickCreateMenu instead.
 */
export default function CenterActionScreen() {
  return <View />
}
